// type=UMD_slides
// course_title=MSML610: Advanced Machine Learning
// lesson_title=L03.2: Propositional and First Order Logic
// version=v1.0 (2026-09-03)
// slides_engine=typst
// references=
// - Russell et al.: _"Artificial Intelligence: A Modern Approach"_ (4th ed, 2020)
//  - Chap 7: Logical agents
//  - Chap 8, First-order logic
//  - Chap 9: Inference in first-order logic
//  - Chap 10, Knowledge representation

// How to run:
// > notes_to_pdf.py --input lectures_source/Lesson03.1-Knowledge_representation.txt --output tmp.pdf --type slides
// > gen_slides.py msml610/03.1 --slides_engine typst
//
// ![](msml610/lectures_source/figures/book_covers/Book_cover_AIMA.jpg){ height=20% }

# Propositional logic

## Syntax

// ## 7.4, Propositional logic (p. 263)

* Propositional Logic
- @Definition@: **Propositional logic** is a formal system for reasoning about
  statements that can be true or false
  - **Syntax** defines the allowable sentences
    - Consists of proposition symbol and logical connectives
    - E.g., $P \land Q$
  - **Semantics** is the way in which the truth of sentences is determined
    - Truth tables or deduction rules evaluate the truth value of complex
      sentences
    - E.g., if $P$ is true and $Q$ is false then $P \land Q$ is false

- @Applications@:
  - _SAT solvers_
    - Tools for determining if a propositional logic formula can be satisfied
    - E.g., used in hardware verification and scheduling problems
  - _Expert systems_
    - Use logic rules to mimic human decision-making
    - E.g., medical diagnosis systems
  - _Rule-based agents_
    - Agents that operate based on a set of predefined rules
    - E.g., automated customer service chatbots

// ## Syntax of propositional logic

* Proposition Symbol
- @Definition@: A **proposition symbol** is an atomic sentence consisting of a
  single symbol
  - E.g., $P$, $Q$, $North$
  - Doesn't have truth value, it is just a symbol for a real-world statement
    (needs grounding for semantics)
  - Stands for a proposition that can be true or false
    - E.g., $K_{E,5}$ = "the Knight is in E5"
    - $K_{E,5}$ is not composed of any other symbol, it is an atomic symbol
  - **Built-in symbols**: $True$ and $False$ are proposition symbols with
    inherent truth values

- @Fact@: Propositional logic is an _atomic representation_ since 
  propositions have no internal structure

* Sentences
- @Definition@: An **atomic sentence** is a sentence composed of a single
  proposition symbol
  - E.g., $P$

- @Definition@: A **complex sentence** is constructed from simpler sentences
  using parentheses and logical connectives
  - E.g., $(P \land Q) \lor R$
  - Recursive definition allows building more complex sentences

- @Definition@: **Logical connectives** are:
  - Not: $\lnot$
  - And: $\land$ (looks like an "A" for "and")
  - Or: $\lor$ (comes from Latin "vel" which means "or")
  - Implies: $\implies$
  - If and only if: $\iff$

- @Key principle@: Each sentence (atomic or complex) can be only true or false

* Proposition Logic: Weather Example
- **Atomic sentences**:
  - $Rain$ = _"it's raining"_
  - $Cold$ = _"it's cold"_
  - $Sunny$ = _"it's sunny"_
  - $Snow$ = _"it's snowing"_
  - $Cloudy$ = _"it's cloudy"_

- _Atomic sentence_ can be positive or negated
  - E.g., $Rain$, $\lnot Rain$ = _"it's not raining"_

- **Negation**
  - E.g., $\lnot (Rain \lor Cloudy)$ = _"it's not the case that it's raining or
    cloudy"_

- **Conjunction / Disjunction**
  - E.g., $Rain \land Cold$ = _"it's raining and it's cold"_
  - E.g., $Rain \lor Snow$ = _"it's either raining or snowing"_

- **Implication** is a sentence containing "premise $\implies$ conclusion"
  - Aka _"if-then statements"_, _"rules"_
  - E.g., $Rain \implies \lnot Snow$ = _"if it's raining, it's not snowing"_

- **Biconditional**: $(A \implies B) \land (B \implies A)$
  - E.g., $Sunny \iff \lnot Cloudy$ = _"it's sunny if and only if it's not
    cloudy"_

* Grammar in BNF form
::: columns
:::: {.column width=50%}
- @Definition@: **Backus Normal Form** formally represents the grammar of
  propositional logic

- @Problem@: _Ambiguity_: the same sentence can be parsed in multiple ways
  - E.g., $\lnot A \lor B = (\lnot A) \lor B$ or $\lnot (A \lor B)$ ?

- @Solution@: Eliminate ambiguity by defining operator precedence
  - E.g., $\lnot$ has higher precedence than $\land, \lor$
  - So $\lnot A \lor B$ means $(\lnot A) \lor B$
::::
:::: {.column width=45%}
![](msml610/lectures_source/figures/L03.propositional_logic_BNF.png){ height=40% }
::::
:::

## Semantics

* Semantics of Propositional Logic
- @Definition@: **Semantics** are rules for determining the truth of a sentence
  $\alpha$ with respect to a model $m$
  - Determine if a sentence is true or false, given a possible world

- @Definition@: **Model definition**: In propositional logic, a model $m$ fixes
  the truth value (true or false) for every atomic sentence
  - The models are **abstractions** of the real world and have no a-priori
    connection to a specific world, e.g.,
    - $P_{1,2}$ is just a symbol and can mean:
      - _"There is a pit in [1, 2]"_ or
      - _"I'm in Paris today and tomorrow"_
    - Need grounding

* Computing the Truth Value of a Sentence
- @Algorithm@: the **truth value of a sentence** is derived from the truth of
  the
  proposition symbols (recursively from the model $m$)
  $$
  m = \{ P_{1,2}=F, P_{2,2}=F, P_{3,1}=T \}
  $$

- All sentences $\alpha$ are constructed from atomic sentences (assigned by
  model
  $m$) and connectives:
  - $\lnot P$ is true iff $P$ is false in $m$
  - $P \land Q$ is true iff $P$ and $Q$ are both true in $m$
  - $P \lor Q$ is true iff $P$ or $Q$ are true in $m$
  - $P \implies Q$ is true unless $P$ is true and $Q$ is false in $m$
  - $P \iff Q$ is true iff $P$ and $Q$ are both true or both false in $m$

- @Definition@: **Truth table** contains truth value of a sentence for each
  possible model $m$
  - E.g., $X = A \land B \lor C$
// TODO(ai_gp): Use a better table and put it on a side with more rows
    ```
    A   B   C   X
    -------------
    F   F   F   F
    F   F   F   T
    ...
    ```

* Interpretation of Implication
- @Fact@: in a logical implication "$P \implies Q$" there is **no causation**
  between $P$ and $Q$
  - "$P \implies Q$" says _"If $P$ is true, I claim that $Q$ is true. Otherwise
    I
    am making no claim"_
    - E.g., _"5 is odd implies that Tokyo is the capital of Japan"_ is a true
      sentence in propositional logic (although very odd)
  - An implication is true whenever the antecedent is false
    - E.g., _"5 is even implies pigs fly"_ is true

* Model Checking is Sound and Complete
- @Remember@: **Model checking algorithm**
  - Enumerates all the models (truth tables)
  - Checks if $\alpha$ is true for every model where $KB$ is true

- @Fact@: in propositional logic, the model checking algorithm is:
  - **Sound**
    - "Any inference made by the algorithm is correct"
    - Implements the definition of entailment
  - **Complete**
    - "Any true sentence is inferred correctly by the algorithm"
    - Works for any $KB$ and $\alpha$
    - Always terminates (finite number of models)

- @Fact@: complexity of model checking with $n$ variables
  - _Time complexity_ is $O(2^n)$ (NP-complete)
    - Worst case is exponential
    - Average case is better than exponential
  - _Space complexity_ is $O(n)$ since enumeration is depth-first

* Inference in Propositional Logic
- @Definition@: **Inference rules** are rules of reasoning (i.e., deriving
  correct statements from the statements of the knowledge base)
  - _Modus Ponens_: if $p \implies q$ and $p$, infer $q$
    - _"If it rains, the ground will be wet"_
    - _"It rains. Therefore, the ground is wet_"
  - _Modus Tollens_: if $p \implies q$ and $\lnot q$, infer $\lnot p$
    - _"If it rains, the ground will be wet. The ground is not wet. Therefore,
      it
      did not rain._"
  - _Syllogism_ (aka transitivity)
    - If $p \implies q$ and $q \implies r$, then $p \implies r$
  - _Disjunctive Syllogism_
    - If $p \lor q$ and $\lnot p$, infer $q$
  - _Addition_: if $p$, then $p \lor q$
  - _Simplification_: from $p \land q$, infer $p$ (or $q$)
  - _Conjunction_: from $p$ and $q$, infer $p \land q$
  - _Resolution Rule_
    - From $(p \lor q)$ and $(\lnot p \lor r)$, infer $(q \lor r)$

* Propositional Theorem Proving
- @Algorithm@: **Prove a sentence** $\alpha$ under a knowledge base KB in
  propositional logic
  - Apply rules of inference to construct a proof of $\alpha$
  - Any sentence can have only one of the following truth values:
    1. True
    2. False
    3. Undecidable under the KB

- @Comparison@
  - _Model checking_ involves enumerating all models to show the sentence is
    true/false in all models where KB is true
  - _Propositional theorem proving_ builds a proof
  - If the proof is short, theorem proving can be more efficient than model
    checking

* Logical Equivalence of Sentences
- **Logical equivalence**: Two sentences $\alpha$ and $\beta$ are logically
  equivalent
  $\alpha \equiv \beta$
  - Iff they are true in the same set of models:
    $$
    M(\alpha) = M(\beta)
    $$
  - Iff they entail each other:
    $$
    \alpha \models \beta \land \beta \models \alpha
    $$
- @Example@: $P \lor Q \equiv Q \lor P$

* Logical Equivalences (1/2)
- **Commutativity** of $\land$ and $\lor$
  $$
  (\alpha \land \beta) \equiv (\beta \land \alpha)
  $$
  $$
  (\alpha \lor \beta) \equiv (\beta \lor \alpha)
  $$
- **Associativity** of $\land$ and $\lor$
  $$
  (\alpha \land \beta) \land \gamma
  \equiv \alpha \land (\beta \land \gamma)
  \equiv \alpha \land \beta \land \gamma
  $$
  $$
  (\alpha \lor \beta) \lor \gamma
  \equiv \alpha \lor (\beta \lor \gamma)
  \equiv \alpha \lor \beta \lor \gamma
  $$
- **Distributivity** of $\land$ over $\lor$
  $$
  \alpha \land (\beta \lor \gamma) \equiv
    (\alpha \land \beta) \lor (\alpha \land \gamma)
  $$
- **Distributivity (dual)** of $\lor$ over $\land$
  $$
  \alpha \lor (\beta \land \gamma) \equiv
    (\alpha \lor \beta) \land (\alpha \lor \gamma)
  $$
- **Double negation** elimination:
  $$
  \lnot (\lnot \alpha) \equiv \alpha
  $$

* Logical Equivalences (2/2)

- **Contraposition**:
  $$
  (\alpha \implies \beta) \equiv (\lnot \beta \implies \lnot \alpha)
  $$
- **Implication elimination**:
  $$
  (\alpha \implies \beta) \equiv (\lnot \alpha \lor \beta)
  $$
- **Biconditional elimination**:
  $$
  (\alpha \iff \beta)
  \equiv (\alpha \implies \beta) \land (\beta \implies \alpha)
  $$
- **De Morgan laws**:
  $$
  \lnot (\alpha \land \beta) \equiv (\lnot \alpha \lor \lnot \beta)
  $$
  $$
  \lnot (\alpha \lor \beta) \equiv (\lnot \alpha \land \lnot \beta)
  $$

* Deduction Theorem
- @Definition@: A **valid sentence** $\alpha$ that is true for all the models
  - E.g., $P \lor \lnot P$
  - Aka "tautology"
  - Every tautology is equivalent to the sentence $True$

- @Definition@: **Contradiction** is a sentence $\alpha$ that is false for all
  the models
  - E.g., $P \land \lnot P$
  - Every contradiction is equivalent to the sentence $False$

- @Theorem@: **Deduction theorem**
  - The sentence $\alpha$ entails $\beta$ (written $\alpha \models \beta$) iff
    the
    sentence $\alpha \implies \beta$ is a tautology

- @Key insight@: The deduction theorem is like a _bridge_ between two different
  but closely related ideas:
  - _Entailment_ $\models$ is a semantic notion
    - It's about truth across all models
    - $\alpha$ entails $\beta$ means in every possible world where $\alpha$ is
      true, $\beta$ is also true
  - _Implication_ $\implies$ is a syntactic notion
    - $\alpha \implies \beta$ is just another formula inside the logic

* Satisfiability
- @Definition@: A sentence $\alpha$ is **satisfiable** iff $\alpha$ is true for
  some model

- @Definition@: **SAT problem** is about determining satisfiability of sentence
  in propositional logic
  - One can enumerate all the possible models until one is found to satisfy the
    sentence $\alpha$
  - It is NP-complete

- @Corollary@: A sentence $\alpha$ is **un-satisfiable** iff $\alpha$ is never
  true (i.e., a contradiction)

- @Corollary@: **Validity and satisfiability**
  - $\alpha$ is valid (i.e., a tautology) iff $\lnot \alpha$ is un-satisfiable
  - By contrapositive, $\alpha$ is satisfiable iff $\lnot \alpha$ is not valid
    ($\lnot \alpha$ is not a tautology)

* Proof by Contraction
- @Definition@: **Proof method**: The sentence $\alpha \models \beta$ is true
  iff
  the sentence
  "$\alpha \lor \lnot \beta)$" is un-satisfiable (i.e., a contradiction)

- @Algorithm@: In a proof by contradiction:
  - Assume $\alpha$
  - Assume that the sentence $\beta$ is false
  - Prove that this leads to a contradiction
  - Thus $\beta$ must be true

// TODO(gp): Finish this

* Propositional Logic: Pros and Cons
- @Pros@
  - Declarative
    - Semantics is based on relation between sentences and possible worlds
  - Can deal with partial information
    - E.g., "A white knight is in `b1` or in `f6`" is represented with
      $WK1_{b1} \lor WK2_{f6}$
  - Compositional semantics
    - The meaning of a sentence is a function of the meaning of its parts
  - Context independent
  - Unambiguous

- @Cons@
  - Can't concisely describe environment with many objects, e.g.,
    - E.g., _"The pawn is in a cell around `b1`"_ requires all the possible
      states to be enumerated
  - Can't represent uncertainty
    - E.g., _"There is 50% of probability that the pawn is in `b1`"_

# First-order Logic

// # 8, First-order logic (p. 264)

## Syntax

* First-Order Logic (FOL)
- @Definition@: **First-order logic** (FOL) extends propositional logic by:
  - Introducing _quantifiers_ (e.g., $\forall$, $\exists$)
  - Using _predicates_ to represent properties and relations

- @Pros@: Combines pros of propositional logic with pros of natural language
  - Built around objects and relations
  - Allows to express facts about some or all objects, e.g.,
     - _"Some humans have green eyes"_
     - _"Chess pieces around the Queen are at risk"_
  - FOL provides expressive power to represent structured, relational knowledge

* First-Order Logic: Syntax

::: columns
:::: {.column width=50%}
- **Constants**: represent specific objects
  - E.g., $Socrates$
- **Predicates**: describe properties or relations
  - E.g., $Human(x)$ = _"x is human"_
- **Functions**: map tuples of objects to objects
  - E.g., $Mother(x)$ = _"the mother of x"_
- **Variables**: act as placeholders
  - E.g., $x, y$
- **Quantifiers**
  - E.g., $\forall x$ (for all $x$)
  - E.g., $\exists x$ (there exists an $x$)
::::
:::: {.column width=50%}

![](msml610/lectures_source/figures/L03.FOL_Syntax.png)

::::
:::

* Sentences
- **Term** is a logical expression that refers to an object
  - E.g., $Richard$

- **Atomic sentence** is a predicate symbol (i.e., corresponds to relations)
  followed by a list of terms in parenthesis
  $Predicate(Term1, Term2, ...)$
  - E.g., $Brother(Richard, John)$ means _"Richard is the brother of John"_,
    under a given grounding / interpretation,  
  - E.g., $Married(Father(Richard), Mother(John))$ means _"the father of Richard
    and the mother of John are married"_

- **Complex sentence** is a sentence using logical connectives complex, with the
  same syntax and semantics as in propositional logic

- **Variable** is a term that represents a possible object
  - Typically represented as lowercase letter (e.g., $x$, $y$, $z$)
  - Can be used as argument of a function, e.g., $LeftLeg(x)$

- **Equality symbol** signifies that two terms refer to the same object
  - E.g., $Father(John) = Henry$

* Quantifiers
- **Quantifiers** express properties of entire collections of objects, instead
  of
  enumerating objects by name (like done in propositional logic)

- **Universal quantifier**: $\forall x\, P(x)$
  - Universal quantifier makes a statement about _every_ object
  - Statement is true if $P(x)$ is true for all $x$

- **Existential quantifier**: $\exists x\, P(x)$
  - Existential quantifier makes a statement about _some_ object (without naming
    it)
  - Statement is true if $P(x)$ is true for at least one $x$

- Variables are **bound** by quantifiers or **free** (unbound)
  - E.g., $\forall x (Cat(x) \rightarrow Mammal(x))$

* Nested Quantifiers
- Complex sentences can be expressed using **multiple quantifiers**
  - The order of quantifiers is important
  - Need to use parentheses to clarify

- @Example@:
  - _"Brothers are siblings"_
    - $\forall x, y \; Brother(x, y) \implies Sibling(x, y)$
  - Siblings is a symmetric relationship
    - $\forall x, y \; Sibling(x, y) \iff Sibling(y, x)$
  - _"Everybody loves somebody"_
    - $\forall x \; \exists y \; Loves(x, y)$
  - _"There is someone loved by everyone"_
    - $\exists y \; \forall x \; Loves(x, y)$

* Connection between $\forall$ and $\exists$
- @Axiom@: the universal and existential quantifiers are **connected** through
  negation and De Morgan rules
// TODO(ai_gp): Use a better latex
  $$
  \forall x \; \lnot P(x) \iff \lnot \exists x: P(x)
  $$
  $$
  \lnot (\forall x \; P(x)) \iff \exists x \lnot P(x)
  $$
  $$
  \forall x \; P(x) \iff \lnot \exists x \lnot P(x)
  $$
  $$
  \exists x \; P(x) \iff \lnot (\forall x \; \lnot P(x))
  $$

## Semantics

* First-order Logic: Semantics
- @Definition@: **Semantics** define how sentences are interpreted in a domain

- Symbols represent entities, relationships, and functions in the domain
  - **Constant symbols** represent specific objects
    - E.g., $Alice$, $GP$, $CS101$

  - **Predicate symbols** represent relationships among objects, e.g.,
    - $EnrolledIn(Student, Class)$
    - $Teaches(Professor, Class)$
    - $IsStudent(x)$, $IsProfessor(x)$

  - **Function symbols** represent mappings between objects
    - E.g., $AdvisorOf(Student)$, $DepartmentOf(Professor)$

- @Definition@: an **interpretation** (aka grounding) maps the world to its
  mathematical description, and vice versa
  - There are many possible interpretations
  - The intended interpretation is often the one that is the most natural
    - E.g., map the symbol $GP$ $\to$ me
    - E.g., $\forall x \, (Human(x) \rightarrow Mortal(x))$ means that every
      human is mortal

* Representing Knowledge in FOL
- @Pros@: FOL enables representation of:
  - General rules: $\forall x\, (Bird(x) \rightarrow CanFly(x))$
  - Specific facts: $Bird(Tweety)$

- **Relations**: Complex relations are captured through predicates:
  - $Loves(Romeo, Juliet)$, $GreaterThan(3, 2)$

- **Functions**: Functions express object construction:
  - $FatherOf(John)$

- **Knowledge base**: Built from axioms and facts

- **Reasoning**: Enables reasoning about objects, properties, and their
  relationships

* First-Order Logic: Inference
- @Goal@: derive new sentences from existing ones using sound rules

  - **Universal Instantiation**:
    - From $\forall x\, P(x)$ infer $P(c)$ for any constant $c$

  - **Existential Instantiation**:
    - From $\exists x\, P(x)$ infer $P(c)$ with a new constant $c$

  - **Standard rules**: Propositional logic rules such as Modus Ponens, Modus
    Tollens, Resolution, etc., also apply within FOL
    - Modus ponens: from $P$ and $P \implies Q$, infer $Q$

- @Complexity@: FOL inference is semi-decidable:
  - If a sentence is entailed, a proof can be found
  - If not entailed, proof search may not terminate
