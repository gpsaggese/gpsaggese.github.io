The problem is how to enforce that Claude Code loads the rules to edit a certain
type of files

Skills don't always trigger and claude.md is read but it also part of the context
which can be forgotten

.claude/hooks/check_file_rules.sh
.claude/settings.json
