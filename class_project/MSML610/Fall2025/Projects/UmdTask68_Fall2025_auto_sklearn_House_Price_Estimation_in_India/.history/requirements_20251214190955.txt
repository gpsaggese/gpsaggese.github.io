# Core data science packages
pandas>=2.0.0
# Modern stack (macOS / Windows / Py3.12+)
numpy>=1.26.0,<2.0.0; platform_system!="Linux" or python_version >= "3.12"
scikit-learn>=1.5.0,<1.6.0; platform_system!="Linux" or python_version >= "3.12"

# Legacy stack for auto-sklearn 0.15 (Linux in Docker)
# auto-sklearn 0.15 requires scikit-learn<0.25 and older NumPy.
numpy>=1.21.0,<1.23.0; platform_system=="Linux" and python_version < "3.12"
scikit-learn>=0.24.0,<0.25.0; platform_system=="Linux" and python_version < "3.12"

# Model persistence
joblib>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Development environment
jupyterlab>=4.1

# AutoML (Linux-only; install via Docker on macOS/Windows)
# Note: auto-sklearn pulls in its own pinned dependencies (smac/ConfigSpace/pyrfr/etc.)
auto-sklearn>=0.15.0; platform_system=="Linux" and python_version < "3.12"

# Gradient boosting
xgboost>=2.0.0
