# Ignore infra directory when building prod container.
ck.infra
# Ignore infra directory when amp is a submodule.
amp/ck.infra
# Ignore docs dir, it can contain large files
docs/
# Ignore Git files, since they can be large.
# Since some of our packages need a Git repo, we initialize an empty Git repo in
# the `prod.Dockerfile`.
.git/
