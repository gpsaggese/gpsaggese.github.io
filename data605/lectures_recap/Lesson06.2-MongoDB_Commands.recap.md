1. Discuss the significance of the `upsert` option in MongoDB's update
   operations. In which scenarios would you prioritize using `upsert` over a
   standard update, and how does this feature enhance data management in
   applications?

2. Compare and contrast the advantages and limitations of using MongoDB as a
   document-oriented NoSQL database versus traditional relational databases
   (RDBMS). In what situations might a developer choose MongoDB, and why might
   they prefer RDBMS for other projects?

3. Reflect on the query capabilities of MongoDB, particularly with the use of
   operators like `$or` and `$gt`. How does the flexibility of MongoDB's
   querying system impact the design of applications that require complex
   querying logic?

4. Explain the practical significance of MongoDB's approach to scaling
   horizontally with commodity hardware. How does this feature influence a
   company's decision to adopt MongoDB for large-scale applications, especially
   in terms of cost and infrastructure?
