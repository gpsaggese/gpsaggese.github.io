1. In the context of SQL, define the roles of different types of statements
   (DDL, DML, DQL, etc.) and provide examples of performance metrics to assess
   how well each type serves its purpose in data management and manipulation.

2. Compare and contrast the effects of using `HAVING` versus `WHERE` in SQL
   queries. In what scenarios would you prefer to use one over the other, and
   why do you think performance could differ between these two clauses?

3. Reflect on the concept of NULL values in SQL. How do NULLs affect the outcome
   of queries, particularly those involving aggregate functions or Boolean
   logic? Discuss the practical implications of handling NULLs when designing a
   database schema.

4. Explain the underlying mechanisms of integrity constraints, particularly
   primary keys and foreign keys. How do these constraints contribute to data
   consistency and referential integrity in relational databases? Provide
   examples of situations where integrity constraints could prevent database
   anomalies.
