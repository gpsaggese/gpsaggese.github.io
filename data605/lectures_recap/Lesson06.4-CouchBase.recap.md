1. Considering the architecture of Couchbase, describe how the separation of
   services (data, index, query, and cluster manager) impacts the overall
   performance and scalability of the database system. In your response,
   identify a real-world application that could benefit from this architecture
   and justify your choice.

2. Compare and contrast CouchDB with other NoSQL databases such as MongoDB in
   terms of their approaches to data consistency and availability. In what
   scenarios might CouchDB’s support for ACID properties give it an advantage
   over MongoDB? Provide specific examples to illustrate your points.

3. Explain how Couchbase’s use of asynchronous data writes can affect data
   reliability and performance. In what situations might it be more beneficial
   to use synchronous writes instead? Discuss the trade-offs involved in these
   scenarios.

4. Analyze the implications of using N1QL for querying JSON documents in
   Couchbase. How does extending SQL to accommodate JSON data structures enhance
   the usability of Couchbase for developers? Discuss potential limitations of
   this approach and provide examples of where alternative query methods may be
   necessary.
